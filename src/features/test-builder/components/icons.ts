import {
    Copy, Trash2, GripVertical, FileCode, Puzzle, Variable, TestTube2, Check,
    FunctionSquare, Pilcrow, Type, Regex, Baseline, AlertTriangle, Fingerprint,
    Construction, Clock, Binary
} from 'lucide-react';

export const AllIcons = {
    Copy, Trash2, GripVertical, FileCode, Puzzle, Variable, TestTube2, Check,
    FunctionSquare, Pilcrow, Type, Regex, Baseline, AlertTriangle, Fingerprint,
    Construction, Clock, Binary
};